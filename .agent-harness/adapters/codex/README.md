# Codex Adapter

Codex uses the root `AGENTS.md` file as its project instruction entry point.

In this harness, `AGENTS.md` points to:

```text
.agent-harness/rules/AGENT_GUIDE.md
```

Add Codex-specific project configuration here only when it is stable and should be shared across repositories.
